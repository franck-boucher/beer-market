import { styled } from "../stitches.config";

const Container = styled("div", {
  maxWidth: 800,
  marginLeft: "auto",
  marginRight: "auto",
  paddingLeft: 20,
  paddingRight: 20,
});

export default Container;
